<%- include('query4') %>

    <div class="col-8 d-flex justify-content-center">
        <div class="chart-container" style="position: relative; height:30vh; width:40vw">
            <canvas id="myChart"></canvas>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
        <script>
            let monthsArray = '<%= monthsInOrder %>'.split(',');
            let dataArray = '<%= avgRentInOrder %>'.split(',');

            const data = {
                labels: monthsArray,
                datasets: [{
                    label: '<%= city %>',
                    backgroundColor: 'rgb(0, 106, 255)',
                    borderColor: 'rgb(0, 106, 255)',
                    data: dataArray,
                }]
            };

            const config = {
                type: 'line',
                data: data,
                options: {
                    plugins: {
                        title: {
                            display: true,
                            text: 'Typical Monthly Trends in <%= city %>, <%= state %> from <%= startingYear %> to <%= endingYear %>'
                        },
                        legend: {
                            display: false
                        }
                    },
                    scales: {
                        x: {
                            title: {
                                display: true,
                                text: 'Month (Combined from <%= startingYear %> to <%= endingYear %>)'
                            }
                        },
                        y: {
                            title: {
                                display: true,
                                text: 'Average Rent Price'
                            }
                        }
                    }
                }
            };
        </script>

        <script>
            const myChart = new Chart(
                document.getElementById('myChart'),
                config
            );
        </script>
    </div>

    </div>
    </div>